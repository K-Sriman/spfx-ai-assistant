// ─── graphSearchService.ts ───────────────────────────────────────────────────
// Legacy Graph /search/query service. Not used in v2 — SharePoint data is now
// read directly via spListService.ts (REST) and answered by GPT-4o.
// Retained for project structure compatibility.
export {};
